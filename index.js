/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {NavegationApp} from './src/NavegationApp';

AppRegistry.registerComponent(appName, () => NavegationApp);
